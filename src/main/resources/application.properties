# JWT CONFIG
# app.jwt.expiration dùng milliseconds. 300000 = 5 phút.
app.jwt.secret=12345678901234567890123456789012
app.jwt.expiration=300000

# COOKIE CONFIG
# Dev local HTTP: secure=false. Production HTTPS: secure=true.
# Production có thể đổi name thành __Host-baixesv_token nếu chạy HTTPS, Path=/ và không set Domain.
app.cookie.name=baixesv_token
app.cookie.secure=false
app.cookie.same-site=Lax

spring.application.name=QuanLiBaiXeSV

spring.datasource.url=jdbc:mysql://localhost:3306/parking_db
spring.datasource.username=root
spring.datasource.password=1111

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
